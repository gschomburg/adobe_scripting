//takes a precomp and time loops it//to start it will take the selected precompfunction loopPreComp(_precompLayer){ //takes in a layer    var fps = 24;    //setup time remapping    _precompLayer.timeRemapEnabled=true;         var timeRemap = _precompLayer.property("Time Remap");    timeRemap.setValueAtTime(0, 0);    timeRemap.setValueAtTime(_precompLayer.outPoint- (1/fps), _precompLayer.outPoint- (1/fps));    //remove the last keyframe for a smooth loop    timeRemap.removeKey(3);    timeRemap.expression = 'loopOut(type = "cycle", numKeyframes = 0)';        //make the precomp longer than 1 loop    _precompLayer.outPoint = 45;    $.writeln(_precompLayer);    }